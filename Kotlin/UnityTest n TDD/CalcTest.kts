import io.kotest.core.spec.style.DescribeSpec
import io.kotest.matchers.shouldBe

class CalcularTests : DescribeSpec({
    describe("Suma") {
        it("Suma los dos numeros dados por las variables"){
            
        }
    }
    describe("Resta") {
        it("Resta el segundo numero dado por las variables al primero"){

        }
    }
    describe("Multiplicación") {
            it("Multiplica los dos numeros dados por las variables"){

            }
    }
    describe("División") {
        it("Divide el segundo numero dado por las variables al primero"){

        }
    }
})